{
  "name": "electron-python-bridge-template",
  "version": "0.1.0",
  "description": "Minimal Electron template that bridges to a persistent Python process via stdio",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
